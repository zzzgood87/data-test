# 🎯 초보자를 위한 실행 가이드

**개발을 처음 하시는 분도 따라할 수 있도록 작성한 가이드입니다.**
천천히 한 단계씩 따라오시면 됩니다! 😊

---

## 📥 1단계: 필수 프로그램 설치하기

### Node.js 설치 (필수)

Node.js는 이 프로그램을 실행하는데 꼭 필요한 프로그램입니다.

#### Windows 사용자

1. **인터넷 브라우저를 열고** 다음 주소로 이동하세요:
   ```
   https://nodejs.org/
   ```

2. **메인 화면에 2개의 버튼이 보입니다:**
   - 왼쪽: "LTS" (추천) - 이 버튼을 클릭하세요!
   - 오른쪽: "Current" - 이건 클릭하지 마세요

3. **다운로드가 시작됩니다:**
   - 파일명: `node-v18.xx.x-x64.msi` 같은 형식
   - 크기: 약 30MB
   - 다운로드 폴더에 저장됩니다

4. **다운로드 폴더를 열어서** 방금 받은 파일을 더블클릭하세요

5. **설치 화면이 나타납니다:**
   - "Next" 버튼을 계속 클릭
   - 중간에 나오는 옵션은 건드리지 말고 기본값 그대로 두세요
   - "Install" 버튼이 나오면 클릭
   - 관리자 권한 요청이 나오면 "예" 클릭
   - 설치가 완료되면 "Finish" 클릭

6. **설치 확인하기:**
   - 키보드에서 `윈도우 키 + R` 을 동시에 누르세요
   - 작은 창이 나타나면 `cmd` 라고 입력하고 엔터
   - 검은 화면(명령 프롬프트)이 나타납니다
   - 다음을 입력하고 엔터:
     ```
     node --version
     ```
   - `v18.xx.x` 같은 숫자가 나오면 성공! ✅
   - 만약 에러가 나오면 컴퓨터를 재시작하고 다시 시도하세요

#### Mac 사용자

1. **Safari나 Chrome을 열고** 다음 주소로 이동:
   ```
   https://nodejs.org/
   ```

2. **왼쪽 "LTS" 버튼을 클릭**하여 다운로드

3. **다운로드 폴더에서** `.pkg` 파일을 더블클릭

4. **설치 진행:**
   - "계속" 버튼을 여러 번 클릭
   - 비밀번호 입력 요청이 나오면 Mac 비밀번호 입력
   - 설치 완료

5. **설치 확인:**
   - `Command + Space` 키를 누르세요
   - `터미널` 이라고 입력하고 엔터
   - 다음을 입력하고 엔터:
     ```
     node --version
     ```
   - 버전 번호가 나오면 성공! ✅

---

## 📂 2단계: 프로젝트 파일 준비하기

### 방법 1: 이미 컴퓨터에 다운로드 받으신 경우

프로젝트 폴더(`data-test`)를 찾으세요. 보통 다음 위치에 있습니다:
- Windows: `C:\Users\사용자이름\Documents\data-test`
- Mac: `/Users/사용자이름/Documents/data-test`

### 방법 2: GitHub에서 다운로드하는 경우

1. **GitHub 페이지로 이동**
2. **초록색 "Code" 버튼 클릭**
3. **"Download ZIP" 클릭**
4. **다운로드된 ZIP 파일을 압축 해제**
5. **압축 해제된 폴더 이름을 `data-test`로 변경**

---

## 💻 3단계: 명령 프롬프트/터미널 열기

### Windows 사용자

**방법 1 (쉬운 방법):**
1. 파일 탐색기에서 `data-test` 폴더를 엽니다
2. 폴더 안의 빈 공간에서 `Shift 키를 누른 채로` 마우스 오른쪽 클릭
3. "여기서 PowerShell 창 열기" 또는 "여기서 명령 프롬프트 열기" 선택
4. 검은색 또는 파란색 창이 열립니다

**방법 2:**
1. `윈도우 키 + R` 누르기
2. `cmd` 입력하고 엔터
3. 검은 창이 열리면 다음을 입력:
   ```
   cd C:\Users\사용자이름\Documents\data-test
   ```
   (사용자이름은 여러분의 윈도우 사용자 이름으로 바꾸세요)

### Mac 사용자

**방법 1 (쉬운 방법):**
1. Finder에서 `data-test` 폴더로 이동
2. 폴더를 마우스 오른쪽 클릭
3. "서비스" → "폴더에서 새로운 터미널 열기" 선택

**방법 2:**
1. `Command + Space` 누르기
2. `터미널` 입력하고 엔터
3. 다음을 입력:
   ```
   cd ~/Documents/data-test
   ```

---

## 🔧 4단계: 백엔드(서버) 설치 및 실행

### 4-1. 백엔드 폴더로 이동

명령 프롬프트/터미널에서 다음을 **한 줄씩** 입력하고 엔터를 누르세요:

```bash
cd backend
```

**확인 방법:**
- Windows: 프롬프트가 `C:\...\data-test\backend>` 로 바뀝니다
- Mac: 프롬프트가 `~/Documents/data-test/backend` 를 포함합니다

### 4-2. 필요한 파일들 설치하기

다음 명령어를 입력하고 엔터:

```bash
npm install
```

**무슨 일이 일어나나요?**
- 화면에 많은 글자들이 빠르게 지나갑니다
- "added XX packages" 같은 메시지가 보입니다
- `node_modules` 라는 폴더가 생성됩니다
- **시간: 약 1~3분 소요** (인터넷 속도에 따라 다름)
- ⚠️ 에러 메시지가 나오지 않으면 정상입니다

**만약 에러가 발생한다면:**
- 인터넷 연결을 확인하세요
- 위 1단계(Node.js 설치)를 다시 확인하세요

### 4-3. 백엔드 서버 실행하기

다음 명령어를 입력하고 엔터:

```bash
npm run dev
```

**성공하면 이런 화면이 나옵니다:**
```
╔════════════════════════════════════════════════╗
║   🏢 부동산 영업 지원 플랫폼 API 서버        ║
║                                                ║
║   포트: 5000                                   ║
║   환경: development                            ║
║   상태: 실행 중                                ║
╚════════════════════════════════════════════════╝
✅ 데이터베이스 연결 성공
```

**🎉 축하합니다! 서버가 실행되었습니다!**

**⚠️ 중요: 이 창은 절대 닫지 마세요!**
- 이 창을 닫으면 서버가 중지됩니다
- 창을 최소화하는 것은 괜찮습니다

---

## 🎨 5단계: 프론트엔드(화면) 설치 및 실행

### 5-1. 새로운 명령 프롬프트/터미널 창 열기

**❗ 기존 창은 그대로 두고, 새 창을 열어야 합니다!**

#### Windows:
1. 다시 `윈도우 키 + R` 누르기
2. `cmd` 입력하고 엔터
3. 새 검은 창이 열립니다

#### Mac:
1. `Command + Space` 누르기
2. `터미널` 입력하고 엔터
3. 새 터미널 창이 열립니다

### 5-2. 프로젝트 폴더로 이동

새로 연 창에서 다음을 입력:

**Windows:**
```bash
cd C:\Users\사용자이름\Documents\data-test\frontend
```

**Mac:**
```bash
cd ~/Documents/data-test/frontend
```

### 5-3. 필요한 파일들 설치하기

다음 명령어를 입력하고 엔터:

```bash
npm install
```

**똑같이 설치 과정이 진행됩니다:**
- 시간: 약 2~5분 소요
- "added XX packages" 메시지가 나오면 성공

### 5-4. 프론트엔드 실행하기

다음 명령어를 입력하고 엔터:

```bash
npm run dev
```

**성공하면 이런 화면이 나옵니다:**
```
  VITE v5.0.8  ready in 500 ms

  ➜  Local:   http://localhost:3000/
  ➜  Network: use --host to expose
  ➜  press h to show help
```

**🎉 성공! 이제 웹사이트가 실행되고 있습니다!**

---

## 🌐 6단계: 웹브라우저에서 접속하기

### 6-1. 웹브라우저 열기

Chrome, Edge, Safari 등 평소에 사용하시는 인터넷 브라우저를 여세요.

### 6-2. 주소창에 입력

브라우저의 주소창(위쪽에 있는 긴 입력칸)에 다음을 입력하고 엔터:

```
http://localhost:3000
```

### 6-3. 로그인 화면 확인

**다음과 같은 화면이 나타나야 합니다:**
- 제목: "부동산 영업 지원 플랫폼"
- 파란색 그라데이션 배경
- 이메일 입력칸
- 비밀번호 입력칸
- "로그인" 버튼
- "계정이 없으신가요? 회원가입" 링크

**👏 완벽합니다! 프로그램이 정상적으로 실행되었습니다!**

---

## 👤 7단계: 첫 사용자 등록하기

### 7-1. 회원가입 페이지로 이동

로그인 화면 하단의 **"회원가입"** 링크를 클릭하세요.

### 7-2. 정보 입력

다음 정보를 입력하세요:

```
이름: 홍길동
이메일: admin@example.com
전화번호: 010-1234-5678
비밀번호: admin1234
비밀번호 확인: admin1234
```

**💡 나중에 사용할 정보이니 메모해두세요!**

### 7-3. 회원가입 버튼 클릭

파란색 **"회원가입"** 버튼을 클릭하세요.

### 7-4. 자동 로그인

회원가입이 완료되면 자동으로 로그인되어 **대시보드 화면**으로 이동합니다.

**대시보드에는 다음이 보입니다:**
- 왼쪽: 지도 (카카오맵)
- 오른쪽: 건물 목록 (현재는 비어있음)
- 우측 하단: 파란색 **+** 버튼

---

## 🏢 8단계: 첫 건물 등록해보기

### 8-1. 건물 등록 시작

우측 하단의 **파란색 + 버튼**을 클릭하세요.

### 8-2. 건물 정보 입력

**건물 등록 폼이 나타납니다. 다음 예시로 입력해보세요:**

```
건물명: 강남빌딩
주소: 서울특별시 강남구 테헤란로 152
```

1. 주소를 입력한 후 **"주소 검색"** 버튼을 클릭하세요
2. "주소로부터 좌표를 찾았습니다!" 메시지가 나옵니다
3. 좌표가 자동으로 입력됩니다 (초록색 체크 표시)

**추가 정보를 입력하세요:**
```
건물 유형: 일반건축물 (기본값)
상태: 정상 (기본값)
총 층수: 10
준공년도: 2020
연면적: 5000
대지면적: 2000
주차대수: 50
설명: 강남역 인근 상업용 건물
```

### 8-3. 건물 등록 완료

하단의 파란색 **"등록"** 버튼을 클릭하세요.

**성공하면:**
- "건물이 등록되었습니다" 알림이 나타납니다
- 건물 상세 페이지로 이동합니다
- 지도에 마커가 표시됩니다

---

## 🏠 9단계: 층/호수 정보 추가하기

건물 상세 페이지에서 **"층별 정보"** 탭을 보고 있어야 합니다.

### 9-1. 유닛 추가 시작

**"+ 유닛 추가"** 버튼을 클릭하세요.

### 9-2. 유닛 정보 입력

**예시로 다음 정보를 입력해보세요:**

```
층수: 5
호수: 501호
면적: 85.5
용도: 사무실
현재 상태: 임대중
월 임대료: 3000000
보증금: 30000000
관리비: 150000
임차인명: (주)ABC기업
임차인 연락처: 02-1234-5678
계약 시작일: 2024-01-01
계약 만료일: 2025-12-31
메모: 남향, 상태 양호
```

### 9-3. 유닛 등록

하단의 **"등록"** 버튼을 클릭하세요.

**성공하면:**
- 층별 정보에 방금 추가한 유닛이 카드 형태로 표시됩니다
- 초록색 "임대중" 배지가 보입니다
- 월세, 보증금 정보가 표시됩니다

**💡 같은 방법으로 다른 층/호수도 추가할 수 있습니다!**

---

## 📝 10단계: 영업 활동 기록하기

### 10-1. 영업 활동 탭으로 이동

건물 상세 페이지 상단의 **"영업 활동"** 탭을 클릭하세요.

### 10-2. 활동 기록 시작

**"+ 활동 기록"** 버튼을 클릭하세요.

### 10-3. 활동 내용 입력

**예시:**
```
활동 유형: 방문
활동 내용:
강남빌딩 5층 현장 방문.
건물 외관 및 내부 상태 확인.
관리실에서 공실 정보 확인.
다음 주 소유주 미팅 예정.

다음 조치 예정일: (오늘로부터 7일 후 날짜 선택)
```

### 10-4. 활동 등록

**"등록"** 버튼을 클릭하세요.

**성공하면:**
- 활동이 타임라인 형태로 표시됩니다
- 방문 아이콘(🚶)과 함께 표시됩니다
- 등록 시간이 자동으로 기록됩니다

---

## 🎯 프로그램 종료하기

### 언제 종료해야 하나요?
작업을 모두 마치고 컴퓨터를 끌 때 종료하세요.

### 종료 방법

**1. 웹브라우저 닫기**
- 브라우저 창을 그냥 닫으면 됩니다

**2. 프론트엔드 서버 종료**
- 두 번째로 연 명령 프롬프트/터미널 창으로 이동
- 키보드에서 `Ctrl + C` 를 누르세요 (Mac: `Command + C`)
- 정말 종료하겠냐고 물으면 `Y` 입력하고 엔터

**3. 백엔드 서버 종료**
- 처음 연 명령 프롬프트/터미널 창으로 이동
- 키보드에서 `Ctrl + C` 를 누르세요 (Mac: `Command + C`)
- 정말 종료하겠냐고 물으면 `Y` 입력하고 엔터

**4. 명령 프롬프트/터미널 창 닫기**
- 두 창 모두 닫으세요

---

## 🔄 다음에 다시 사용하려면?

### 빠른 재실행 가이드

**매번 npm install을 다시 할 필요는 없습니다!**
한 번만 설치하면 됩니다.

**다음부터는 이렇게만 하세요:**

**1단계: 백엔드 실행**
```bash
# 명령 프롬프트/터미널 열기
cd C:\Users\사용자이름\Documents\data-test\backend  (Windows)
cd ~/Documents/data-test/backend                     (Mac)

# 서버 실행
npm run dev
```

**2단계: 프론트엔드 실행**
```bash
# 새 명령 프롬프트/터미널 열기
cd C:\Users\사용자이름\Documents\data-test\frontend  (Windows)
cd ~/Documents/data-test/frontend                     (Mac)

# 실행
npm run dev
```

**3단계: 브라우저 접속**
```
http://localhost:3000
```

**4단계: 로그인**
- 이메일: admin@example.com
- 비밀번호: admin1234

---

## ❓ 자주 묻는 질문 (FAQ)

### Q1: "npm을 찾을 수 없습니다" 에러가 나요
**A:** Node.js 설치가 제대로 안 된 것입니다.
- 1단계로 돌아가서 Node.js를 다시 설치하세요
- 설치 후 컴퓨터를 재시작하세요

### Q2: 포트가 이미 사용중이라고 나와요
**A:** 이미 프로그램이 실행 중입니다.
- 기존에 열어둔 명령 프롬프트/터미널을 찾아서 종료하세요
- 컴퓨터를 재시작하면 확실히 해결됩니다

### Q3: 지도가 안 보여요
**A:** 카카오맵 API 키 문제입니다.
- 이미 키가 적용되어 있으니 괜찮습니다
- 혹시 안되면 브라우저 새로고침(F5)을 해보세요

### Q4: 로그인 정보를 잊어버렸어요
**A:** 데이터베이스를 초기화하면 됩니다.
1. 두 서버 모두 종료
2. `backend` 폴더에서 `database.sqlite` 파일 삭제
3. 서버 재시작
4. 다시 회원가입

### Q5: 모바일에서도 사용할 수 있나요?
**A:** 네! 같은 WiFi에 연결된 경우:
1. 컴퓨터의 IP 주소 확인
   - Windows: `ipconfig` 명령어
   - Mac: 시스템 환경설정 → 네트워크
2. 모바일 브라우저에서 접속:
   ```
   http://컴퓨터IP주소:3000
   예: http://192.168.0.10:3000
   ```

### Q6: 데이터가 날아갈까요?
**A:** 아니요, 안심하세요!
- 모든 데이터는 `backend/database.sqlite` 파일에 저장됩니다
- 프로그램을 종료해도 데이터는 남아있습니다
- 단, 이 파일을 삭제하면 데이터가 사라집니다

---

## 📞 도움이 더 필요하신가요?

**에러 메시지를 정확히 복사해서 문의하세요:**
1. 어느 단계에서 에러가 발생했는지
2. 화면에 나타난 빨간색 에러 메시지 전체 내용
3. Windows인지 Mac인지

**백업 꿀팁:**
정기적으로 `backend/database.sqlite` 파일을 복사해서 안전한 곳에 보관하세요!

---

## 🎉 축하합니다!

이제 부동산 영업 지원 플랫폼을 완전히 사용할 수 있습니다!

**다음 단계:**
- ✅ 건물 더 추가하기
- ✅ 층별 유닛 상세 정보 입력하기
- ✅ 영업 활동 꾸준히 기록하기
- ✅ 지도에서 건물 찾아보기
- ✅ 팀원들과 함께 사용하기

**즐거운 부동산 영업 되세요! 🏢💼**
